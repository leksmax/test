--- a/net/mac80211/tx.c
+++ b/net/mac80211/tx.c
@@ -4510,6 +4510,9 @@ __ieee80211_beacon_get(struct ieee80211_
 	txrc.bss = true;
 	rate_control_get_rate(sdata, NULL, &txrc);
 
+	if (unlikely(local->in_reconfig))
+		return false;
+
 	info->control.vif = vif;
 
 	info->flags |= IEEE80211_TX_CTL_CLEAR_PS_FILT |
