#!/bin/sh

start(){
	echo "start traffic meter"
}

stop(){
	echo "stop traffic meter"
}

restart(){
	stop
	start
}

case "${1}" in
	start)
		start
	;;
	stop)
		stop
	;;
	restart)
		restart
	;;
	*)
		echo $"Usage: $0 {start|stop|restart}"
		exit 1
esac
